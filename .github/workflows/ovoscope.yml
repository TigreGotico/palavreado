name: E2E Tests (ovoscope)

on:
  pull_request:
    branches: [dev]
    paths:
      - 'palavreado/**'
      - 'test/test_ovoscope_e2e.py'
      - 'test/test_ovoscope_hierarchical_e2e.py'
      - 'test/end2end/**'
      - '.github/workflows/ovoscope.yml'
  workflow_dispatch:

jobs:
  e2e:
    uses: OpenVoiceOS/gh-automations/.github/workflows/ovoscope.yml@dev
    with:
      install_extras: "test"
      test_path: "test/test_ovoscope_e2e.py test/test_ovoscope_hierarchical_e2e.py test/end2end/"
      require_palavreado: true
      # Validate against the unreleased ovoscope + palavreado dev branches
      # (needed for the E2EPipelineHarness used by the refactored e2e tests).
      # Drop this once ovoscope and palavreado have been released to PyPI.
      pre_release: true
